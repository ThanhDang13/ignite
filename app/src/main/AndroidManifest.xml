<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AlarmApp"
        android:name=".AlarmApplication">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.AlarmApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.example.alarm.feature.ring.RingActivity"
            android:exported="true"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:taskAffinity="com.example.alarm.ring"
            android:theme="@style/Theme.AlarmApp" />

        <receiver
            android:name="com.example.alarm.data.scheduler.AlarmReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="com.example.alarm.ALARM_TRIGGER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.example.alarm.data.scheduler.PreAlarmReceiver"
            android:exported="false"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.example.alarm.PRE_ALARM_TRIGGER" />
                <action android:name="com.example.alarm.DISMISS_PRE_ALARM" />
                <action android:name="com.example.alarm.SKIP_ALARM" />
                <action android:name="com.example.alarm.SNOOZE_PRE_ALARM" />
            </intent-filter>
        </receiver>

        <service
            android:name="com.example.alarm.feature.ring.RingService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback" />

        <service
            android:name="com.example.alarm.feature.timer.TimerService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <receiver
            android:name="com.example.alarm.feature.ring.RingNotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.alarm.NOTIFICATION_DISMISS" />
                <action android:name="com.example.alarm.NOTIFICATION_SNOOZE" />
            </intent-filter>
        </receiver>

        <receiver
            android:name="com.example.alarm.feature.widget.AlarmWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.alarm.REFRESH_WIDGET" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/alarm_widget_info" />
        </receiver>

    </application>

</manifest>
